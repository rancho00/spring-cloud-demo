spring:
  profiles:
    active:
      -dev
---
spring:
  profiles: dev
  application:
    name: user-provider
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

---
spring:
  profiles: prod
  applicaiont: prod
